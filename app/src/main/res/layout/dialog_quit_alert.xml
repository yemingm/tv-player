<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dialog_back"
    android:orientation="vertical">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/dialog_info_margin_top"
        android:text="确定要退出吗？"
        android:textColor="@android:color/white"
        android:textSize="@dimen/text_channel_item" />


    <Button
        android:id="@+id/quitButton"
        android:layout_width="@dimen/button_width"
        android:layout_height="@dimen/button_height"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/button_margin_v"
        android:layout_marginLeft="@dimen/button_margin_h"
        android:background="@drawable/bt_background_selector"
        android:text="确定"
        android:textColor="@android:color/white"
        android:textSize="@dimen/text_dialog_bt" />

    <Button
        android:id="@+id/cancelButton"
        android:layout_width="@dimen/button_width"
        android:layout_height="@dimen/button_height"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="@dimen/button_margin_v"
        android:layout_marginRight="@dimen/button_margin_h"
        android:background="@drawable/bt_background_selector"
        android:text="取消"
        android:textColor="@android:color/white"
        android:textSize="@dimen/text_dialog_bt" />

</RelativeLayout>